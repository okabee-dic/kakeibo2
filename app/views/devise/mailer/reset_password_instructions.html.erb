<p><%= @resource.email %>様</p>

<p>Kakeibo運営です。</p>
<p>これより、パスワードのリセットを行います。下のリンクを進むとパスワードを変更します。</p>

<p><%= link_to 'パスワードを変更する', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>もしこのメールに心当たりがないようでしたら、こちらもメールは無視しても問題ありません。</p>
<p>上のリンクを進まなければ、これまで通りのパスワードでログインすることができます。</p>